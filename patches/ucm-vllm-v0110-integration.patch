diff --git a/ucm/integration/vllm/patch/v0110/vllm/sparse/attention/layer.py b/ucm/integration/vllm/patch/v0110/vllm/sparse/attention/layer.py
index baeefed..92ee834 100644
--- a/ucm/integration/vllm/patch/v0110/vllm/sparse/attention/layer.py
+++ b/ucm/integration/vllm/patch/v0110/vllm/sparse/attention/layer.py
@@ -11,6 +11,7 @@ from vllm.forward_context import ForwardContext, get_forward_context
 from ucm.sparse.state import (
     maybe_execute_sparse_attention_begin,
     maybe_execute_sparse_attention_finished,
+    maybe_execute_sparse_attention_should_skip,
 )
 
 
@@ -66,17 +67,18 @@ def unified_attention_with_output(
                 query, key, value, layer_name, forward_context, output, k_hash=k_hash
             )
 
-    self.impl.forward(
-        self,
-        query,
-        key,
-        value,
-        kv_cache,
-        attn_metadata,
-        output=output,
-        output_scale=output_scale,
-        output_block_scale=output_block_scale,
-    )
+    if not maybe_execute_sparse_attention_should_skip():
+        self.impl.forward(
+            self,
+            query,
+            key,
+            value,
+            kv_cache,
+            attn_metadata,
+            output=output,
+            output_scale=output_scale,
+            output_block_scale=output_block_scale,
+        )
 
     if not self.use_mla:
         maybe_execute_sparse_attention_finished(
diff --git a/ucm/integration/vllm/patch/v0110/vllm/sparse/model_executor/models/qwen2.py b/ucm/integration/vllm/patch/v0110/vllm/sparse/model_executor/models/qwen2.py
index 21a0c34..a578542 100644
--- a/ucm/integration/vllm/patch/v0110/vllm/sparse/model_executor/models/qwen2.py
+++ b/ucm/integration/vllm/patch/v0110/vllm/sparse/model_executor/models/qwen2.py
@@ -12,6 +12,8 @@ from ucm.sparse.state import (
     maybe_execute_sparse_ffn_finished,
     maybe_execute_sparse_layer_begin,
     maybe_execute_sparse_layer_finished,
+    maybe_execute_sparse_model_finished,
+    maybe_get_sparse_layer_end,
 )
 
 
@@ -82,8 +84,11 @@ class Qwen2Model(nn.Module):
             residual = intermediate_tensors["residual"]
 
         aux_hidden_states = []
+        effective_end_layer = maybe_get_sparse_layer_end(
+            self.start_layer, self.end_layer
+        )
         for idx, layer in enumerate(
-            islice(self.layers, self.start_layer, self.end_layer)
+            islice(self.layers, self.start_layer, effective_end_layer)
         ):
             if idx in self.aux_hidden_state_layers:
                 aux_hidden_states.append(hidden_states + residual)
@@ -103,6 +108,9 @@ class Qwen2Model(nn.Module):
                 {"hidden_states": hidden_states, "residual": residual}
             )
 
+        hidden_states, residual = maybe_execute_sparse_model_finished(
+            hidden_states, residual
+        )
         hidden_states, _ = self.norm(hidden_states, residual)
 
         if len(aux_hidden_states) > 0:
diff --git a/ucm/integration/vllm/patch/v0110/vllm/sparse_patch.py b/ucm/integration/vllm/patch/v0110/vllm/sparse_patch.py
index ffeef33..2493bd6 100644
--- a/ucm/integration/vllm/patch/v0110/vllm/sparse_patch.py
+++ b/ucm/integration/vllm/patch/v0110/vllm/sparse_patch.py
@@ -50,6 +50,20 @@ def patch_qwen2_model(mod):
     patch_or_inject(mod.Qwen2Model, "forward", qwen2.Qwen2Model.forward)
 
 
+@when_imported("vllm.model_executor.models.qwen3")
+def patch_qwen3_model(mod):
+    logger.debug(f"Patched {mod} called")
+
+    from ucm.integration.vllm.patch.v0110.vllm.sparse.model_executor.models import (
+        qwen2,
+        qwen3,
+    )
+
+    patch_or_inject(mod.Qwen3Attention, "forward", qwen3.Qwen3Attention.forward)
+    patch_or_inject(mod.Qwen3DecoderLayer, "forward", qwen3.Qwen3DecoderLayer.forward)
+    patch_or_inject(mod.Qwen3Model, "forward", qwen2.Qwen2Model.forward)
+
+
 @when_imported("vllm.v1.attention.backends.mla.common")
 def patch_common_attention_backend(mod):
     logger.debug(f"Patched {mod} called")
diff --git a/ucm/integration/vllm/ucm_connector.py b/ucm/integration/vllm/ucm_connector.py
index 4e0087d..29fce8d 100644
--- a/ucm/integration/vllm/ucm_connector.py
+++ b/ucm/integration/vllm/ucm_connector.py
@@ -18,9 +18,18 @@ from vllm.distributed.kv_transfer.kv_connector.v1.base import (
     KVConnectorBase_V1,
     KVConnectorMetadata,
     KVConnectorRole,
-    SupportsHMA,
 )
 
+try:
+    from vllm.distributed.kv_transfer.kv_connector.v1.base import SupportsHMA
+except ImportError:
+    # SupportsHMA was added after vLLM 0.11.
+    from abc import ABC
+
+    class SupportsHMA(ABC):
+        pass
+
+
 try:
     from vllm.distributed.kv_transfer.kv_connector.v1.base import (
         KVConnectorWorkerMetadata,
@@ -28,6 +37,21 @@ try:
 except ImportError:
     KVConnectorWorkerMetadata = object
 
+
+def _kv_connector_base_kwargs(
+    vllm_config: "VllmConfig",
+    role: KVConnectorRole,
+    kv_cache_config: Optional[Any],
+) -> dict:
+    """Build base-connector arguments accepted by the installed vLLM."""
+    import inspect
+
+    kwargs = {"vllm_config": vllm_config, "role": role}
+    if "kv_cache_config" in inspect.signature(KVConnectorBase_V1.__init__).parameters:
+        kwargs["kv_cache_config"] = kv_cache_config
+    return kwargs
+
+
 from vllm.distributed.parallel_state import get_world_group
 from vllm.model_executor.models.utils import extract_layer_index
 from vllm.platforms import current_platform
@@ -322,7 +346,10 @@ class KVCacheLayout:
             name: extract_layer_index(name) for name in kvcaches.keys()
         }
         self.first_layer_id = next(iter(self.layer_name_to_id.values()))
-        self.num_blocks = self.kv_cache_config.num_blocks
+        # vLLM 0.11 does not pass kv_cache_config to connectors.
+        self.num_blocks = (
+            self.kv_cache_config.num_blocks if self.kv_cache_config is not None else 0
+        )
         self._build_layout(kvcaches)
 
     def _collect_tensor_rows(self, kvcaches):
@@ -1049,9 +1076,7 @@ class UCMDirectConnector(KVConnectorBase_V1):
         kv_cache_config: Optional["KVCacheConfig"] = None,
     ):
         super().__init__(
-            vllm_config=vllm_config,
-            role=role,
-            kv_cache_config=kv_cache_config,
+            **_kv_connector_base_kwargs(vllm_config, role, kv_cache_config)
         )
         self.use_layerwise = False
         self.kv_caches: dict[str, torch.Tensor] = {}
@@ -2783,9 +2808,7 @@ class UCMConnector(KVConnectorBase_V1, SupportsHMA):
         kv_cache_config: Optional["KVCacheConfig"] = None,
     ):
         super().__init__(
-            vllm_config=vllm_config,
-            role=role,
-            kv_cache_config=kv_cache_config,
+            **_kv_connector_base_kwargs(vllm_config, role, kv_cache_config)
         )
         self.connector: KVConnectorBase_V1
         ucm_config = Config(vllm_config.kv_transfer_config)
@@ -2830,6 +2853,17 @@ class UCMConnector(KVConnectorBase_V1, SupportsHMA):
             )
             return
 
+        ucm_sparse_config = (
+            self.launch_config.get("ucm_sparse_config", {}) or {}
+            if self.launch_config is not None
+            else {}
+        )
+        if isinstance(ucm_sparse_config, dict) and "QcFuse" in ucm_sparse_config:
+            from ucm.integration.vllm.qcfuse_connector import UCMQcFuseConnector
+
+            self.connector = UCMQcFuseConnector(vllm_config, role, kv_cache_config)
+            return
+
         pp_enabled = self._vllm_config.parallel_config.pipeline_parallel_size > 1
         if pp_enabled and not use_layerwise:
             raise RuntimeError(
@@ -3114,6 +3148,12 @@ class UCMConnector(KVConnectorBase_V1, SupportsHMA):
         """
         self.connector.wait_for_layer_load(layer_name)
 
+    def setup_model(self, model) -> None:
+        """Delegate optional model setup to the selected connector."""
+        setup_model = getattr(self.connector, "setup_model", None)
+        if callable(setup_model):
+            setup_model(model)
+
     def save_kv_layer(
         self,
         layer_name: str,
diff --git a/ucm/sparse/base.py b/ucm/sparse/base.py
index a13266a..2541d03 100644
--- a/ucm/sparse/base.py
+++ b/ucm/sparse/base.py
@@ -229,6 +229,26 @@ class UcmSparseBase(ABC):
         """
         pass
 
+    def should_skip_impl_forward(self) -> bool:
+        """Return whether the sparse hook already computed attention."""
+        return False
+
+    def qwen3_pre_rope(
+        self,
+        query: torch.Tensor,
+        key: torch.Tensor,
+        value: torch.Tensor,
+        positions: torch.Tensor,
+        layer_id: int,
+        rotary_emb,
+    ) -> Tuple[torch.Tensor, torch.Tensor, torch.Tensor, bool]:
+        """Optionally transform Qwen3 Q/K/V before rotary embedding."""
+        return query, key, value, False
+
+    def effective_layer_end(self, start_layer: int, end_layer: int) -> int:
+        """Return the exclusive decoder-layer end for the current step."""
+        return end_layer
+
     def update_states(self, scheduler_output: SchedulerOutput) -> None:
         """
         This is called at the beginning of "ModelRunner->execute_model" function.
@@ -274,6 +294,12 @@ class UcmSparseBase(ABC):
         """
         return positions, hidden_states, residual
 
+    def model_finished(
+        self, hidden_states: torch.Tensor, residual: torch.Tensor
+    ) -> Tuple[torch.Tensor, torch.Tensor]:
+        """Restore sparse model state before the final normalization."""
+        return hidden_states, residual
+
     def request_finished_in_worker(self, request_id: Union[int, str]):
         """
         This function releases the resources of finished requests at worker-side.
diff --git a/ucm/sparse/factory.py b/ucm/sparse/factory.py
index 1f66855..66b045b 100644
--- a/ucm/sparse/factory.py
+++ b/ucm/sparse/factory.py
@@ -54,3 +54,4 @@ UcmSparseFactory.register_sparse_method(
     "KVStarMultiStep", "ucm.sparse.kvstar.multistep", "KVStarMultiStep"
 )
 UcmSparseFactory.register_sparse_method("Blend", "ucm.sparse.blend.blend", "Blend")
+UcmSparseFactory.register_sparse_method("QcFuse", "ucm.sparse.qcfuse.qcfuse", "QcFuse")
diff --git a/ucm/sparse/state.py b/ucm/sparse/state.py
index 493a890..3960096 100644
--- a/ucm/sparse/state.py
+++ b/ucm/sparse/state.py
@@ -108,6 +108,15 @@ def maybe_execute_sparse_ffn_finished(
     return ucm_spare.ffn_finished(hidden_states, residual)
 
 
+def maybe_execute_sparse_model_finished(
+    hidden_states: torch.Tensor, residual: torch.Tensor
+):
+    if not has_ucm_sparse():
+        return hidden_states, residual
+    ucm_spare = get_ucm_sparse()
+    return ucm_spare.model_finished(hidden_states, residual)
+
+
 def maybe_execute_sparse_attention_begin(
     query: torch.Tensor,
     key: torch.Tensor,
@@ -143,6 +152,40 @@ def maybe_execute_sparse_attention_begin(
     )
 
 
+def maybe_execute_sparse_attention_should_skip() -> bool:
+    if not has_ucm_sparse():
+        return False
+    ucm_sparse = get_ucm_sparse()
+    return ucm_sparse.should_skip_impl_forward()
+
+
+def maybe_execute_sparse_qwen3_pre_rope(
+    query: torch.Tensor,
+    key: torch.Tensor,
+    value: torch.Tensor,
+    positions: torch.Tensor,
+    layer_id: int,
+    rotary_emb,
+):
+    if not has_ucm_sparse():
+        return query, key, value, False
+    return get_ucm_sparse().qwen3_pre_rope(
+        query, key, value, positions, layer_id, rotary_emb
+    )
+
+
+def maybe_get_sparse_layer_end(start_layer: int, end_layer: int) -> int:
+    if not has_ucm_sparse():
+        return end_layer
+    layer_end = get_ucm_sparse().effective_layer_end(start_layer, end_layer)
+    if not start_layer <= layer_end <= end_layer:
+        raise ValueError(
+            f"Invalid sparse layer range [{start_layer}, {layer_end}) "
+            f"inside [{start_layer}, {end_layer})"
+        )
+    return layer_end
+
+
 def maybe_execute_sparse_attention_finished(
     query: torch.Tensor,
     key: torch.Tensor,
diff --git a/ucm/store/factory_v1.py b/ucm/store/factory_v1.py
index 0c248ed..4ca63de 100644
--- a/ucm/store/factory_v1.py
+++ b/ucm/store/factory_v1.py
@@ -68,3 +68,6 @@ UcmConnectorFactoryV1.register_connector(
 UcmConnectorFactoryV1.register_connector(
     "UcmPipelineStore", "ucm.store.pipeline.connector", "UcmPipelineStore"
 )
+UcmConnectorFactoryV1.register_connector(
+    "UcmQcFuseStore", "ucm.store.qcfuse.qcfuse_store", "UcmQcFuseStore"
+)
